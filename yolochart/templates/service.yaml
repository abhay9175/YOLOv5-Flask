apiVersion: v1
kind: Service
metadata:
  name: {{ include "yolochart.fullname" . }}
  labels:
    {{- include "yolochart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "yolochart.selectorLabels" . | nindent 4 }}
