apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "yolochart.fullname" . }}-test-connection"
  labels:
    {{- include "yolochart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "yolochart.fullname" . }}:{{ .Values.service.port | grep 80 }}']
  restartPolicy: Never
